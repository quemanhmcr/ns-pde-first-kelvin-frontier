# NS PDE-first Kelvin Frontier

Standalone local Git repository for the PDE-first, structure-first 3D Navier--Stokes regularity research programme developed in this conversation.

## Research rule

Let the Navier--Stokes PDE and its physical/stochastic current structure act first. Before estimates, classify every term as one of: physical current/transfer, exact pressure or gauge form, connection/holonomy geometry, martingale quadratic variation/future variance, Hodge/spatial flux, ancestry deformation, quantile/localization covariance, physical exit, or observer artifact.

Every result must be labeled as **Exact identity**, **Rigorous consequence**, **Audited calibration**, **Heuristic**, **Conjectural bridge**, or **Open-literal/Open**. No regularity claim is allowed before the continuation/restart bridge is actually closed.

## Current frontier

The selected Kelvin quadratic variation has an exact fixed-current future-variance bank, but no selector-independent finite bank has been found at the present one-/two-ancestry level. On the **full diffusion state**, the canonical continuous same-ancestor branching tensor is the viscous diagonal tensor `2 nu q K delta_Delta`; drift traffic `q j_circ tensor j_circ` is a different physical object.  If the stored ancestry state is reduced relative to the physical Kelvin current-shape state, a separate conditional resolution-covariance pair face is mandatory.

The surviving target is an exact **pair-localization current/capacity law** for the migrating first-bad germ.  The active selector has now been type-corrected: it acts on a library of closed Kelvin cycles, not on arbitrary ambient physical chains.  On this intrinsic domain its physical boundary residual and full-pair physical boundary residual vanish exactly; germ support transport is an exact cut-interface current, and finite hysteresis jumps are exact covariance revaluations.

For the cycle-typed selector sector the current identity has no intrinsic active-selector defect:

`Pi_sel - Pi_dist = boundary(pair localization world-sheet) + quantile + shell + physical exit + connection + reset`,

with full cross-shell and cross-child content retained inside the physical maps.  The stochastic Cauchy deformation sector is now coupled to this current algebra without retyping either factor: for a selected current map `P`, the local spatial lift is exactly `P tensor D^T`, while the physical boundary is `B tensor I`, so `(B tensor I)(P tensor D^T)=(BP) tensor D^T`.  A closed selected cycle therefore remains closed under every deformation replica and on the full ordered pair boundary.  Full `Sigma_D=Cov(vec D)` projects exactly to fixed local tangent/cochain covariance; its `2 nu/3` cubic onset survives that projection.  If two replicas use different selectors, however, the pair difference contains separate selector and deformation sectors plus mandatory cross pair terms.  If they share the frozen first-bad selector, the selector and cross sectors vanish exactly.  This is only a local tangent/current theorem: exact cubic and higher polynomial NS heat shears still rule out descent of a finite Kelvin current to `D` (or any finite shape-moment truncation) alone.  The next full-state layer is now literal as well: on `(r,X,R(.),D)` only the anchor `X` carries Brownian q.v.; relative shape and `D` are finite variation.  Navier--Stokes makes the moving momentum-one-form drift a Bernoulli/pressure exact form on closed currents, while Cartan turns Brownian anchor translation into the Kelvin coefficient `<i_e Omega,Z>`.  Consequently `Sigma_D`, Kelvin variance, and `Cov(vec D,K_Z)` are diagonal/off-diagonal blocks of one same-ancestor connected covariance.  Their smooth short-age onsets are respectively `h^3`, `h`, and the forced mixed `h^2`, with a single leading Gram-integral representation.  What remains open is not the source: it is the first-bad finite-shape-to-local descent under genuinely shrinking physical support.  The finite-shape hierarchy has now been reduced further without truncation: with the actual reverse local line frame `L_dot=-(grad u)L`, `xi=L^-1 r` and `a_tilde=cof(L)^-1 a`, all affine shape transport cancels and the entire pulled-back oriented-moment tower is driven by the single divergence-free nonaffinity field `N_L=L^-1[u(X+L xi)-u(X)-(grad u)L xi]`.  Coherent linear refinement is exact gauge for this tower; homogeneous degree-p jets enter as `rho^(p-1) S^-1 U_p(S xi)` when `L=rho S`.  Exact linear strain shows a frozen codeforming tower need not be support-local, while exact quadratic heat-shear with shrinking `L=diag(r^3,r,r)` gives `N_L=r^-1 xi_y^2 e_x`, so support locality alone does not force codeforming affine collapse.  The literal next seam is therefore joint physical-support plus anisotropic jet/frame control, not another finite moment closure.  A further exact Stokes--Piola reduction sharpens the target: with `G=L^T L`, the finite Kelvin descent error is `epsilon_K=oint (G N_L).dxi` and `curl_xi(G N_L)=cof(L)^T[omega(X+Lxi)-omega(X)]`.  Thus bare kinematic `N_L` and the Kelvin one-form `G N_L` are distinct physical faces.  Exact quadratic heat shear on `L=diag(r^3,r,r)` has `N_L~r^-1` but `G N_L~r^5`; dynamic shape-affinity and instantaneous Kelvin descent must therefore remain separate seams.  The metric-whitened Stokes topology now has an exact physical meaning as well: for `H=cof(L)`, pointwise `H^-T curl_xi(G N_L)=omega(X+Lxi)-omega(X)`, while at finite orientation-complete scale `r_H=H^-T epsilon_H` is the physical residual reconstructed from three different face circulations.  Its energy/covariance are exactly the existing packet-metric contractions, but full whitened payoff covariance retains mandatory local/residual cross blocks.  Exact cubic heat shear gives zero center defect yet nonzero unit-cube reconstruction and the exact isotropic `r^2` whitened decay.  This closes fixed-state topology typing, not uniform first-bad collapse or any cross-clock future-bank bridge.  The reconstruction has now also been differentiated on the literal reverse-age state: for the local-frame error `epsilon=K-H^T omega`, exact NS gauge plus Nanson/stretching cancellation makes `epsilon` a pure orientation-coordinate martingale, while `r=H^-T epsilon` carries the physical line connection `dr=-(grad u)r dsigma+sqrt(2nu)Qhat dW`.  The earlier actual-area shape drift `-omega.R_A` is not lost; it transfers exactly into `omega.(h_R-h_local)` with opposite drift.  Full reconstructed q.v./dyad dynamics retain the signed local/residual cross blocks.  Cubic NS has nonzero residual with zero q.v.; one-mode NS activates cross q.v., so no single positive residual bank captures the dynamics.  Pulling once more by the actual reverse line frame gives the smallest law yet: with `J=det L` frozen by incompressibility, `eta=L^-1 omega`, `chi=L^-1 r=epsilon/J`, and `kappa=K/J=eta+chi` are all driftless same-anchor martingales.  Their complete second-moment dynamics is one joint Gram tensor; physical strain reappears only as metric work in the pushforward `r=L chi`.  `E chi` is conserved while covariance grows by q.v., so bias and stochastic spread are rigorously distinct.  Cubic NS has nonzero constant bias with zero q.v.; a full-period one-mode face has positive local and residual q.v. diagonals canceled exactly by negative cross q.v. to give zero full-circulation q.v.  A further admissibility audit now shows that idempotency is not needed: any linear or differentiable CK operation that genuinely acts on Kelvin currents must preserve closed cycles, in which case it produces no intrinsic physical-boundary or pair-content defect when full pair content is retained.  If it breaks closedness, exact pressure gauge exposes a physical open boundary/interface/exit instead.  The literal global `S^int / Z_irr` equivalence remains open because `S^int` itself has never been defined line by line.

The finite-shape descent seam is now literal rather than schematic.  The previous shorthand `K_{Z(R)}-K_{Z_local(D,H)}` was overtyped: the actual reverse-current area has local connection `+A^T`, while the same-replica Cauchy metric-dual frame `H_C=rho^2 D^-1` has `-A^T`, so they are not the same geometry.  With the actual finite-surface area vector `h_R`, the exact Stokes descent error is `epsilon_K=K_{Z(R)}-omega(X).h_R`.  On the full reverse-age state it obeys `d epsilon_K=-omega.R_A dsigma+sqrt(2nu) sum_mu q_mu^err dW_mu`, where `R_A` is the finite strain-gradient shape current and `q_mu^err` is the finite-support vorticity-gradient residual.  Pathwise `[vec D,epsilon_K]=0`, while finite-horizon `Cov(vec D,epsilon_K)` is the ordinary off-diagonal connected-covariance block generated by common anchor sampling.  Exact cubic heat-shear NS gives a nonzero **conserved** finite-shape error with zero drift, zero q.v., zero variance, and zero deformation/error covariance; exact ABC activates the finite-variation shape-drift face; exact one-mode shear activates the stochastic error q.v. and `D` cross covariance.  The polynomial/Legendre hierarchy shows every finite even-moment truncation can miss a higher deterministic flux mode at a symmetry point where the error q.v. coefficient is zero.  Thus covariance alone cannot prove local descent: the remaining first-bad theorem must control the actual deterministic shape bias/current together with support locality and covariance remainder.

The full oriented surface-moment hierarchy is now literal as well.  For `M_alpha=int r^alpha n dA`, reverse-age material transport gives `Mdot_alpha=-sum_i alpha_i int r^(alpha-e_i) Delta u_i n dA+int r^alpha (grad u(X+r))^T n dA`.  Affine velocity is the exact order-preserving exception; a homogeneous spatial velocity jet of degree `p>=2` couples moment order `m` to order `m+p-1`.  Exact quadratic heat-shear NS already destroys material-anchor centering by feeding a centered first moment from the quadrupole, while in the complementary `xy` shear geometry the entire oriented `y`-moment tower is conserved exactly.  A single anchor shift changes the vector-valued first-moment matrix only by `F -> F-c h^T`, so generic transverse first-moment content cannot be centered away.  This turns the old finite-shape obstruction into a dynamic theorem: no fixed finite low-moment state is a universal exact quotient of nonlinear material-surface transport.  The surviving question is genuinely infinite-dimensional but physical: whether the actual migrating first-bad support enforces a uniform scale/shape law that collapses the whole moment tower asymptotically.

The restart frontier has now been tied back to the literal vorticity PDE and upgraded from one loop to an **orientation-complete three-loop packet**.  The full shared-noise packet q.v. is the `3 x 3` Gram matrix `Gamma_mf=2 nu N^T (grad omega)(grad omega)^T N`; exact ABC flow already has negative cross-orientation entries, so diagonal orientation bookkeeping is physically incomplete.  For a general invertible material area frame `H`, the scalar packet capacity `B=(1/2) tr(C (H^T H)^(-1))` is exactly GL(3)-invariant: passive rotation, anisotropic dilation, and shear cancel when full covariance is retained.  The local future-covariance tensor is no longer merely an ansatz: on the **full stochastic Kelvin state**, vector conditional moments obey the exact matrix bank law `(partial_tau-L) C = Gamma[m]`, with the same full mixed carré-du-champ that appears as the same-ancestor pair diagonal defect.  Double Stokes identifies small-loop future covariance with the diagonal density of `(d box d) K_s`, where `K_s` is the already existing pair momentum covariance cochain; conditional mean-square continuity gives a rigorous fixed-state tensor limit only after the packet is also support-local and its error is small in the metric-whitened topology `H^{-T} epsilon_H -> 0`.  For centered genuinely local, uniformly conditioned conditionally `C^2` packets the raw non-tensorial remainder starts at `r^6`, hence contributes only `r^2` after metric normalization.  Navier--Stokes itself gives the stronger tensor identity `D_K(omega omega^T)=-2 nu grad(omega) grad(omega)^T`, while the causal backward-Kelvin shear covariance gains the same tensor with the opposite sign, so q.v. is an exact transfer from conditional mean-square into covariance.  The full current-shape kinematics is now literal as well: under the uniform backward Wiener flow an anchor carries all Brownian q.v. while relative loop/surface shape has finite variation driven by velocity differences.  Exact differential area elements close on `(x,H)` by Nanson, but exact finite surfaces do not: the smooth Navier--Stokes cubic heat shear `u=(y^3+6 nu t y,0,0)` gives two centered surfaces with the same anchor and the same area vector but different `H` drift, with the missing term equal to a physical strain-gradient/surface-quadrupole current.  Its centered raw scaling is `r^4`, hence relative-to-area `r^2`.  The normalized ancestry time orientation is now also explicit at operator level: expanding `L=w.grad+nu phi^(-1) div(phi K grad)` gives a forward Itô drift `b_+=w+nu c_phi`, time reversal gives `b_-=w-nu c_phi-2nu K grad log f`, and the stored current velocity is exactly `j=(b_++b_-)/2`.  Hence the old time-orientation seam reduces to a literal **state-semantics/lift** question.  If the ancestry state is full physical Kelvin state, its measure calculus must allow the exact degenerate shape support seen in affine shear.  If it is reduced, a conditional lift kernel `kappa(y,dY_K)` and its exact resolution covariance are mandatory.  What remains open is declaring/constructing that ancestry semantics and lift, together with **uniform singular-time collapse/control of the full finite-shape hierarchy and metric-whitened covariance remainder**, plus physical refinement/support, boundary, exit, and reset work.  An independent adversarial audit further shows that `H->0` is not itself packet locality: incompressible anisotropic deformation can keep support diameter order one while every area-frame entry vanishes.  The fixed-state future-tensor theorem is therefore repaired to require support locality and metric-whitened conditional `L^2` error `H^{-T} epsilon_H -> 0`; raw Frobenius remainder smallness is insufficient.  On the ancestry side, `phi,f,w` have an exact reference gauge leaving `q,j,L,b_+,b_-` invariant, so the true state-map domain is the still-undefined ancestry state manifold with gauge-invariant `(K,b_-)`.  Since Kelvin relative shape has zero q.v., a full-rank ancestry diffusion cannot encode nontrivial smooth shape without deterministic/null or history state.  The coherent microcell geometry has also collapsed to a rigid three-factor core `L=rho F`, `H=rho^2 F^-T`, `T_tot=F Q_tot F^T`: incompressible material flow changes `F` but not `rho`, physical refinement changes `rho`, and Kelvin q.v. is internal transfer inside `Q_tot`.  Support and vorticity dyads share the same two-sided stretch operator; in co-deforming coordinates the stretch cancels, while exact polynomial heat shears prove that no finite surface-moment truncation closes finite shape universally.  The Kelvin remaining horizon supplies the physical parabolic support scale `sqrt(2 nu tau)`, but the first-bad selector still has no theorem identifying its germ scale with that parabolic scale.  The clock/cut audit now separates same-clock diffusion reversal from reversal of a future-bank clock: physical reverse age `sigma` has the exact full-state generator `L_K,rev=-K^-`, so a flat identity-map future-bank bridge requires `b_+=-u`, whereas `b_-=u` is a distinct same-clock condition.  Fixed-mass level-set quantiles obey the exact coarea/current law and reduce in one dimension to `a_dot=j(a,t)`; the remaining first-bad cut seam is no longer an unknown speed formula but the missing definition of the scalar germ observable/threshold and its outer physical-time/state lift.  In exact affine linear-strain NS, the reverse-age Gaussian covariance is `Sigma=2 nu int exp(-Ar)exp(-A^T r) dr`, the time-integrated reverse support Cauchy--Green tensor, and Mahalanobis quantile shells satisfy `g_sigma+j.grad g=0` pointwise.  Thus quantile-shell geometry and material-support geometry are already the same physical deformation system in this calibration.  Draft PR #1 has now also been incorporated as an adversarial compatibility repair: the covariance PDE is **not** treated as an ordinary de Rham exact one-form, but as the exact Dynkin/Fokker--Planck spacetime current `partial_s(qV)+div(qjV+nu q K grad V)=-q gamma`; moving quantile/shell cuts retain the distinct `Qdot`/boundary-speed face and its two-replica lift; and the fixed-current bank is explicitly a same-Markov-clock theorem.  The physical first-bad time/state to reverse-age Kelvin lift and the programme-specific moving-cut instantiation remain open-literal.  A further selector-semantics audit separates two previously conflated data types: `M_fb` is a hysteretic event selector, coordinate-piecewise-constant until `resolved`, whereas `Q_s/H_s` are moving localization maps with Reynolds faces.  The generic quantile-current speed law is already exact, but the repo still supplies both `bad_flags` and `resolved` as Boolean oracles and does not define the separate scalar quantile/shell observable.  Exact amplitude-scaled periodic ABC flow now excludes a large class of naive replacements for those Boolean oracles: raw finite thresholds on vorticity, enstrophy, instantaneous stretching, Kelvin bulk q.v., or their instantaneous ratio can all be crossed arbitrarily in a globally smooth exact periodic NS family, so none can alone certify continuation failure.  A subsequent support×bank audit gives a more meaningful **scale-parametric** conditional route: for any coherent physical scale `ell`, `P_ell=ell^2 F F^T` and `Q_tot=eta eta^T+Ctilde` satisfy an exact three-face PSD factorization, so aligned Loewner envelopes imply `|omega|^2<=pq/ell^2`.  Specializing `ell^2=2 nu(Theta-t)` yields a conditional local `tau^-1/2` rate only after a same-state covariance family is paired with that shrinking scale.  Causal fixed-past backward-Kelvin covariance instead has horizon `h=t-t0`; matching `h=Theta-t` requires the moving terminal `t0(t)=2t-Theta` and adds an exact terminal-motion face.  Thus scale/covariance horizon identification, uniform/global first-bad envelopes, and continuation remain open.  Returning to a genuinely fixed past terminal gives a sharper physical bank anatomy: the stochastic Cauchy payoff is `Y=D omega(A_s^t,s)`, so `omega omega^T<=Q_s<=W_s R_s` with `W_s=sup|omega(s)|^2` and `R_s=E[D D^T]`; moreover `W_s R_s-omega omega^T=(W_s R_s-Q_s)+(Q_s-omega omega^T)` splits terminal directional headroom from centered covariance.  Exact affine-vortex NS has zero centered covariance but deformation carries all stretching, whereas one-mode shear activates covariance with no vorticity-direction stretching.  Thus a fixed-past bank does not remove the obstruction: it retypes it as stochastic Cauchy deformation geometry plus directional correlation.  On each stochastic replica this deformation geometry is not new: with `F_C=D^T` and coherent area frame `H_C=rho^2 F_C^-T`, one has exactly `D D^T=rho^4 (H_C^T H_C)^-1`.  Thus the fixed-past deformation moment is the expectation of the same unscaled packet metric already present in the restart geometry; only the deterministic first-bad selected packet ↔ stochastic replica alignment remains open-literal.  Exact one-mode shear now shows why that alignment cannot be equality: at `y=0` the deterministic selected deformation is exactly identity, while Brownian backward replicas sample off-axis strain.  The full matrix-valued deformation covariance is now kept as `Sigma_D=Cov(vec D)`; the packet-metric face is only its row-Gram projection `C_D^Gram=E[DD^T]-Dbar Dbar^T`, so `rho^4 E[M_rep]=Dbar Dbar^T+C_D^Gram`.  On the causal reverse-age horizon `h=t-s`, the exact connected law is `H_h Sigma_D=(I tensor (grad u)^T)Sigma_D+Sigma_D(I tensor grad u)+Gamma_D^vec`, where `H_h=partial_h+partial_t+u.grad-nu Delta` and `Gamma_D^vec=2nu sum_mu vec(partial_mu Dbar) vec(partial_mu Dbar)^T`.  Thus locally at a smooth current point `Sigma_D=(2nu/3)h^3 sum_mu vec((partial_mu grad u)^T) outer vec((partial_mu grad u)^T)+O(h^4)`, while `C_D^Gram=(2nu/3)h^3 sum_mu (partial_mu grad u)^T(partial_mu grad u)+O(h^4)`.  Exact one-mode shear referees the positive sign, transpose, matrix ordering and coefficient `2nu/3`; exact affine-vortex NS referees the zero spatial-gradient source.  This is finite-variation deformation dispersion generated by Brownian anchor sampling, not pathwise q.v. of `D`.  It is an exact same-clock observable covariance sector on the augmented causal reverse-age state.  Cross-module audit now shows that, after `B_conn=-(I tensor (grad u)^T)^T`, this is exactly the repo's existing connected vector covariance/pair-defect theorem on the reverse-age generator; only the physical payload/clock is new.  If an explicit reduced lift hides deformation, the exact vector law of total covariance adds `Cov_R(Dbar_vec)` as a separate resolution face rather than retyping `Sigma_D`.  None of this identifies the future-remaining bank, `S^int`, irreducible content, or the actual ancestry lift; those cross-clock/reduction/selected-support bridges remain open-literal.

See `docs/clock_cut_compatibility_audit.md`, `docs/ancestry_time_reversal_audit.md`, `docs/kelvin_shape_generator_audit.md`, `docs/future_covariance_tensor_audit.md`, `docs/orientation_complete_restart_packet.md`, `docs/vorticity_kelvin_restart_audit.md`, `docs/cycle_typed_first_bad_selector.md`, `docs/kelvin_ck_admissibility_audit.md`, and `docs/selected_kelvin_pair_localization_budget.md`, and `docs/finite_shape_kelvin_descent_audit.md`, `docs/surface_moment_hierarchy_audit.md`, `docs/codeforming_surface_moment_tower_audit.md`, `docs/codeforming_whitened_kelvin_remainder_audit.md`, `docs/dynamic_reconstructed_kelvin_residual_audit.md`, and `docs/reverse_codeforming_kelvin_martingale_audit.md`.

## Current audited refinement result

The pair-localization audit has now isolated an exact refinement rule: a linear
physical refinement `Z_P=sum_i a_i Z_i` must be lifted by the **full tensor square**
`sum_ij a_i a_j Z_i tensor Z_j`.  Cross-child covariance is physical refinement
content, not a Pillar-II defect.  Exact odd-mode periodic Navier--Stokes shear
calibrations in GitHub Actions witness complete cancellation between positive child
diagonals and negative cross-child covariance for the parent `Z_0+Z_pi`.

See `docs/pair_localization_worldsheet_audit.md`.

### Latest: physical weighting corrects the raw codeforming first-bad target

The reverse-age martingale coordinate `chi` is not itself the physical finite-to-local topology.  The actual reconstructed residual is `r=L chi`, hence the exact physical energy is `chi^T L^T L chi`.  Exact quadratic and one-mode Navier--Stokes shears show that raw codeforming bias and raw codeforming q.v. can remain order one while the physical residual/noise shrink.  If `L` is random, a signed metric--residual correlation is mandatory; `E[M]E[chi chi^T]` does not close the full state.  The first-bad weighted residual collapse and support locality remain open and distinct.

### Latest: directional and event balance of the physical Kelvin residual

The physical residual energy now has an exact principal-line ledger `sum_i sigma_i^2 v_i^T Q_chi v_i`; finite events split exactly into geometry reweighting and current/residual revaluation, with a third metric--residual correlation face on the random full state.  Passive GL reparameterization can have nonzero signed faces but zero total change.  Exact quadratic Navier--Stokes also shows weighted residual collapse can occur while one support line stays order one, so support locality and residual descent remain distinct open first-bad requirements.

### Latest: pathwise spectral Kelvin-residual channels

The random-frame weighted residual can be kept fully literal without factorizing geometry from residual content: pathwise `M=sum lambda_alpha P_alpha` gives `E=sum lambda_alpha tr(P_alpha Q)`.  For simple spectrum, every principal channel has exactly three smooth traffic faces—eigenvalue stretch, residual content, and eigenframe mixing—and the total mixing equals the off-diagonal metric work.  At spectral degeneracy, individual axes are gauge and the eigenspace projector is the canonical object.  Exact linear Navier--Stokes shear activates this mixing mechanism.  Principal-channel collapse and support locality remain separate open first-bad requirements.

The current selected-lineage layer now keeps the first-bad germ factor and physical residual fiber separate.  The literal rank-one `M_fb tensor I_3` commutes with per-germ block spectral operators, so frozen first-bad selection creates no spectral commutator source.  Finite selector changes and any explicitly supplied common-fiber linear residual refinement use the full tensor-square synthesis map; exact one-mode Navier--Stokes shows cross-child spectral content can cancel positive child diagonals and that positive quadratic reset path length is not a physical bank.  Endpoint projector blocks are canonical, but individual principal-axis matching across events and the actual first-bad current-to-residual refinement lift remain open-literal.

The refinement seam has narrowed again.  For an orientation-complete linear current-packet map with raw blocks `R_i`, circulation and oriented-area readouts use the same blocks, so the raw finite error refines exactly.  Whitening then uniquely forces the physical residual blocks `A_i=H_P^-T R_i H_i^T`; for coherent `H=cof(L)` this becomes `A_i=(J_i/J_P)L_P R_i L_i^-1`, and in codeforming coordinates all anisotropic conjugation cancels to `B_i=(J_i/J_P)R_i`.  The full second moment and spectral channels therefore inherit the exact `A tensor A` / `B tensor B` pair functor with all cross-child terms.  What remains open-literal is no longer a whitening lift: it is the actual moving first-bad orientation-packet refinement map `R_i`, together with support locality, moving-cut clocks, and ancestry/future-bank identification.

For the scalar physical refinement class already present in the pair world-sheet, `Z_parent=sum_i w_i Z_i`, the orientation-complete lift is canonical: `R_i=w_i I_3`, so `R_i tensor R_j=w_i w_j I_9`, and the exact interval chain-map subdivision also tensors with `I_3`.  Hence orientation-preserving scalar refinement is no longer an open packet-lift case.  The remaining literal event question is whether the actual first-bad reselection/refinement stays in this class or supplies a genuinely orientation-mixing block map.

Finite-event principal-channel lineage no longer needs a canonical axis matching.  With the frame-aware physical blocks `A_i`, every parent spectral projector channel is exactly the signed sum `lambda_P tr(P_P A_i P_i Q_ij P_j A_j^T)` over all ordered child pairs and child projector blocks.  This projector law is regular at eigenvalue degeneracy and invariant under internal basis changes of a degenerate block.  Exact half-period one-mode Navier--Stokes activates positive same-child traffic cancelled by negative cross-child traffic, so the exact event law is not a positive diagonal child-channel kernel.  The remaining open-literal issue is the actual first-bad event map/state, not cross-event eigenvector ancestry.

The finite-event algebra now has a gauge normal form.  For fixed invertible endpoint frames, raw orientation blocks `R` and physical residual blocks `A=H_parent^-T R H_child^T` determine each other exactly; codeforming `B=(J_child/J_parent)R` is the equivalent volume-normal form.  Sequential events compose by ordinary matrix multiplication, their full pair maps compose by the tensor-square functor, and any complete intermediate spectral projector resolution telescopes out.  A diagonal scalar channel list is not a compositional state: two positive-semidefinite second moments can have identical diagonal channels but different cross-channel coherence and therefore different responses under the same next event.

Within the unrestricted finite linear event-probe class, the full symmetric second moment is observationally complete at second order: coordinate probes recover the diagonal entries and pair-sum probes recover every off-diagonal entry by polarization.  This sharpens the no-go for diagonal channel vectors without claiming that the actual first-bad event family realizes all such probes or that full `Q` is minimal for that programme-specific family.

The hysteretic first-bad selector is now typed separately from physical packet transport at the reconstructed-residual level.  The active residual is the extraction `E_g X` from a persistent germ/fiber library.  For a genuine switch `g != h`, there is no universal matrix `T` with `E_h=T E_g`; two full library states can have the same old selected residual or second moment and different switched values.  The exact second-moment reset re-enters the cross-germ and hidden new-germ blocks of the full pair library.  A selected-to-selected transition exists only after an additional admissible-state relation is proved.

For a specified finite physical packet library carried by one stochastic-flow replica, the persistent residual dynamics are now exact: stack the packet noise responses into one `3N x 3` matrix `Qstack`, then `dChi=sqrt(2nu) Qstack dW` and the full germ-pair q.v. is the single Gram `2nu Qstack Qstack^T`.  Cross-germ blocks are direct common-noise q.v., not optional covariance corrections, and the instantaneous source has rank at most three.  Exact one-mode Navier--Stokes gives opposite same-replica packet noises whose negative cross-germ q.v. cancels both positive diagonals; assigning independent Brownian drivers per germ gives the wrong synthesized q.v.  The remaining open-literal seam is the actual first-bad candidate-library/clock/replica instantiation.

The active first-bad residual path now has an exact same-clock hybrid semimartingale law once a same-replica library and piecewise-constant selector path are supplied.  On frozen branches it inherits the selected Brownian Gram; at a selector-only reset `DeltaY=(E_+-E_-)Chi`.  The selector creates no continuous Brownian carré-du-champ source, but the càdlàg optional quadratic variation includes the finite jump square `DeltaY DeltaY^T`.  That jump square is observer path variation, not a covariance bank: an exact one-mode Navier--Stokes closed selector excursion returns to the identical selected residual while accumulating strictly positive jump q.v.

A simultaneous physical packet event and selector switch is now literal too.  If the persistent library changes by a specified linear map `A` while the active readout changes from `E_-` to `E_+`, the selected jump operator is exactly `D=E_+ A-E_-`.  On a common library space `D=E_- DeltaA+DeltaE+DeltaE DeltaA`: the last term is a mandatory physical--selector interaction, not a Brownian source.  The selected second-moment jump keeps the full left/right/quadratic pair faces with this combined `D`.  Exact one-mode Navier--Stokes residuals under a specified hidden-germ synthesis show that dropping `DeltaE DeltaA` gives the wrong selected jump.  What remains open-literal is the actual NS first-bad timing and event-map/state instantiation, not the simultaneous-event algebra.

The continuous Brownian source across such an event is now typed separately from the càdlàg jump q.v. atom.  If `N` is the stacked same-replica noise response, then the adjacent selected interval rates are `2nu(E_-N)(E_-N)^T` and `2nu(E_+AN)(E_+AN)^T`; their signed finite revaluation has the same full left/right/quadratic pair product rule with `D=E_+A-E_-`.  This rate change is not the jump square and not a negative carré-du-champ when signed.  Exact one-mode Navier--Stokes gives a hidden synthesis for which selector-only q.v. rate appears unchanged, while the actual post-event noise cancels exactly and the continuous source rate drops to zero.

At expectation level an adaptive event map cannot be replaced by its mean.  For two replicas the exact congruence law splits `E_2[C Q C^T]` into mean-map/mean-payload, positive event-map dispersion, and two signed event--state correlation faces.  PSD calibrations give both `1+1+1+1=4` and `1+1-1-1=0`, so the correlation faces can reinforce or exactly cancel the positive pieces.  The same law applies to state second moments and same-replica q.v. Grams.  Thus an actual first-bad adaptive rule requires the joint event-map/library law; the rule itself remains Open-literal.

Before proposing any badness functional, the repository now enforces necessary physical admissibility constraints.  Passive packet `GL(3)` coordinates leave `r=H^-T epsilon` and its physical energy invariant, while a raw residual norm can flip the identity of the “worst” germ under a basis change with no physical change at all.  Physical event maps are likewise gauge invariant only after the raw event block transforms equivariantly.  These corrections are necessary but not sufficient: exact quadratic Navier--Stokes has physical residual energy `rho^2 -> 0` while one support line remains exactly length one.  Genuine selector switches additionally require persistent-library memory, later linear events require full signed second-moment coherence rather than diagonal channel scores, and adaptive rules require the joint event-map/state law.  The actual first-bad badness/resolve functional remains Open-literal.

The surviving local enstrophy growth gate has now been typed directly from the PDE.  At an enstrophy critical point, `partial_t e = omega.S.omega - nu|grad omega|^2 + nu Delta e`; the middle loss is exactly the metric-normalized orientation-complete Kelvin q.v. bulk.  At a local maximum, positive `stretching-Kelvin-bulk` margin is necessary but not sufficient for growth because the nonpositive curvature face must also be overcome.  A global Beltrami identity shows every ABC enstrophy critical point has zero stretching scalar, so the amplitude-scaled ABC family cannot referee this gate.  Exact affine vortex NS realizes positive gate and exact smooth enstrophy growth with zero Kelvin bulk, but is nonperiodic/non-finite-energy and is therefore only a local mechanism calibration, not a target-class regularity counterexample.

A nondegenerate moving enstrophy critical point now has an exact speed law as well.  Differentiating `grad e(x_*(t),t)=0` and inserting the three-face PDE balance gives `H_e(xdot_*-u)+grad[omega.S.omega-nu|grad omega|^2+nu Delta e]=0`.  Thus the maximum is generally not material; its relative velocity is driven by the gradients of stretching, Kelvin bulk, and curvature through the inverse enstrophy Hessian.  Exact periodic ABC gives a strict maximum at `(pi/4,pi/4,pi/4)` with nonzero fluid velocity but `xdot_*=0`, while exact affine uniform enstrophy has `H_e=0` and no unique critical speed.  The formula is therefore conditional on a nondegenerate critical branch and is not yet the programme first-bad cut-speed law.

The critical-Hessian evolution is now exact too.  Along a differentiable critical branch, `Hdot = Hess(R)-(grad u)^T H-H grad u+((xdot_*-u).grad)H`, with `R=stretching-Kelvin_bulk+nu Delta e`.  The connection splits into strain reshaping plus a rotation commutator.  Its `log|det H|` rate is exactly `-2 div u`, so incompressibility removes local linear deformation from curvature-volume change without freezing Hessian shape.  On a nondegenerate branch `d log|det H|/dt=tr(H^-1 Hdot)`; finite integrated log-rate preserves nonzero determinant conditionally.  Exact periodic ABC has `Hdot=-2nu H`, `d log|det H|/dt=-6nu`, and remains nondegenerate for every finite time.  Critical branch degeneracy remains a genuine event seam and is not a Navier--Stokes continuation theorem.

Critical-candidate ranking has now been separated from branch degeneracy.  For two critical values the gap rate is the difference of their literal stretching/Kelvin-bulk/curvature rates.  A geometric calibration shows a transverse value tie is independent of full Hessian degeneracy.  More importantly, an exact periodic three-mode Navier--Stokes heat shear has two persistent critical sheets `y=0,pi` whose enstrophy values cross at `t=1/nu` while both transverse curvatures remain strictly negative.  Both values are decreasing; the switch is driven purely by different curvature-decay rates, with gap rate `48 nu e^-2`.  The non-hysteretic selected maximum is continuous at the tie but its derivative switches.  Thus winner-switch, branch birth/death, and physical packet events are distinct typed events; mapping any of them to the actual first-bad hysteresis remains Open-literal.


## 2026-08-13 draft PR #2 adjudication: own-local Kelvin events

Draft PR #2 exposed one genuine event-interface correction and two anti-extension guardrails.  The frame-aware `A/B` residual synthesis remains exact for a **common local vorticity target**.  A packet library with packet-specific anchors instead has the exact affine event

\[
\varepsilon_P=\sum_iR_i\varepsilon_i+\Delta_\omega,\qquad
\Delta_\omega=\sum_iR_iH_i^T(\omega_i-\omega_P),
\]

so `r_P=A r+d` and the continuous Brownian response has the matching target-gradient face.  The target offset is an exact coboundary and composes functorially for supplied finite current events.  The existing two-replica adaptive four-face identity also survives exactly, but a general population has an additional centered triple face.  Likewise, the finite-event selected semimartingale law survives on its supplied locally-finite-jump domain; an endogenous selector still needs a local-finiteness theorem, summable limit, or interface/local-time law.

See `docs/own_local_kelvin_affine_event_audit.md` and `docs/pr2_adjudication_2026-08-13.md`.  These results do **not** define the physical first-bad rule, prove support locality, identify the restart clock, or prove continuation/global regularity.

A literal physical critical-sheet merger has now been instantiated in the Kelvin packet library.  The exact periodic two-mode NS heat shear has side enstrophy sheets `y=pi+-d` merging into the persistent `y=pi` sheet at `t*=1/nu` while the velocity/vorticity field stays analytic.  For an explicitly supplied translation-covariant fixed-shape one-sided box packet, support/frame/circulation/own-local target/residual/nonzero anchor-noise all coalesce, the branch-resolved library event is `A=E_0`, and the physical collision forces `d=0` and `N_target=0`.  The common nonzero packet noise gives identical same-replica cross-label q.v. blocks, so normalized collision quotients preserve q.v. only with those cross blocks.  But the same merged critical sheet admits different valid packet shapes with different area frames, residuals, and noise; therefore critical position/enstrophy coalescence alone does **not** force full Kelvin packet-state coalescence.  A selector label jump can have zero physical state jump at collision while the side packet branch rate diverges through the critical-coordinate speed.  Branch ancestry, actual first-bad badness/resolve identification, general endogenous selector local finiteness, and every restart/continuation/regularity bridge remain Open-literal/Open.

The merger packet has now also been transported instead of frozen.  Exact NS immediately separates three physical objects that the fixed-shape picture could not distinguish: the Eulerian critical sheet, a Kelvin/material ancestry trajectory, and a sheet-attached Nanson frame.  In the exact shear `u_y=0` while the side critical sheets have nonzero normal speed, and a literal Kelvin anchor has y q.v. rate `2 nu` while the deterministic critical path has zero q.v.; hence critical-sheet attachment is a moving-cut/reanchoring readout, not a Kelvin ancestry path.  Along the reanchored local Nanson connection, `gamma_dot=-q` and the branch gap obeys the rigid exact square `d/dt(gamma_0-gamma_s)=q_s-q_0=-e^{-nu t}(1-r)^2/(2r)<0`, so equal pre-merger frames reach the same anchor/vorticity with different support/frame histories.  In this shear the circulation and reconstructed residual can nevertheless coincide, proving that residual-fiber coalescence does not imply full history-carrying packet coalescence.  Finally the earlier singular packet cusp is now identified exactly as the area-normalized Reynolds moving-cut circulation flux; the viscous face stays finite and the isolated side cut has finite total variation.  Thus literal critical-path/ancestry identification is ruled out in this calibration and the surviving bridge must be a nontrivial selector/readout/lift.  The programme first-bad rule, cross-clock ancestry/future-bank lift, uniform singular-time collapse, restart, and continuation remain Open-literal/Open.

The ancestry/readout seam is now composed rather than identified.  A reduced physical Kelvin ancestry state may lift to the full current-shape state through a conditional kernel, while an Eulerian selector is a moving restriction of that ancestry population.  At second order the selected covariance is exactly the sum of intrinsic full-Kelvin covariance, hidden-state resolution covariance, and localization covariance of the conditional mean.  Reynolds transport acts on all three by one signed boundary-mass revaluation and creates no independent Brownian selector source.  In the exact periodic critical-sheet merger, the Kelvin anchor has a stationary uniform `y` marginal while the chamber `[pi-d,pi]` shrinks with `d_dot=-3 nu cot d`.  Although the boundary speed diverges, exact criticality forces `q_side-q_mean=O(d^4)` and selected vorticity variance `O(d^8)`; consequently the normalized mean rate is `O(d^2)` and the variance rate is `O(d^6)`.  The latter splits exactly into the intrinsic `-2 nu <|grad q|^2>` Kelvin bulk and a distinct signed moving-cut covariance revaluation.  Thus the supplied physical moving-readout semantics is closed without an extra selector source; the programme-specific first-bad observable/localization, global ancestry lift, two-clock identification, uniform singular-time control, restart, continuation and regularity remain open.

The first-bad localization layer now has an endogenous Navier--Stokes candidate rather than a Boolean oracle.  With `e=|omega|^2/2`, `M(t)=max_x e`, and `g=e/M`, the complete superlevel filtration `{g>=theta}` is similarity-neutral.  On every regular boundary its exact outward speed relative to the fluid is `(omega.S.omega-nu|grad omega|^2+nu Delta e-theta Mdot)/|grad e|`; after dividing by the forced powers of `M`, both the compatibility numerator and the boundary speed are NS-similarity invariant.  Feeding this boundary into an ancestry continuity law produces only the fluid/probability-current mismatch and the intrinsic level-slip flux, with no selector force or Brownian source.

Exact periodic referees distinguish magnitude from compatibility.  Integer-frequency one-mode heat shears make raw max enstrophy grow like `n^4` and Kelvin bulk like `n^6` while `g=cos^2(ny)` is stationary and the compatibility defect is identically zero.  A separate exact four-mode heat shear has two genuine global enstrophy maxima at `y=0,pi` crossing at `t=1/nu`; an exact polynomial certificate gives `M=18`, both transverse Hessians are negative, and the scalar max envelope has zero value jump but one-sided rates `-336nu` and `-240nu`.  Thus the scalar normalization does not require a locally-finite branch selector.  Identification of this intrinsic filtration with actual continuation failure, full packet selector dynamics, uniform support/finite-shape collapse, restart, and regularity all remain Open-literal/Open.


The intrinsic max-normalized enstrophy filtration has now been lifted onto the same orientation-complete Kelvin/Nanson support state.  Exact periodic one-mode Navier--Stokes gives a sharp transverse-locality no-go: for nested chambers `theta=cos^2(alpha)->1`, the normal chamber span, packet volume, physical Kelvin residual, target-gradient q.v., finite-shape endpoint defect, and local Nanson line rate collapse while the tangent-plane support tensor stays `P B P=diag(1,0,1)` and one oriented area face stays exactly order one.  That surviving face has zero Kelvin circulation/target/error because its normal is orthogonal to the vorticity, while its face quadrupole retains both transverse support directions.  Thus scalar compatibility plus Kelvin-readout collapse does not imply packet support locality; the exact missing geometry face is tangential support/refinement, whose actual first-bad collapse remains Open-literal.  No restart/continuation/regularity theorem is claimed.

The transverse-support no-go has now been sharpened into an intrinsic curvature--support kernel law. At a max-normalized enstrophy critical point, `Q=-Hess(g)` satisfies `D_t Q+A^T Q+Q A=K`, while physical line transport `D_t L=A L` gives the connection-free identity `D_t(L^T Q L)=L^T K L`. The connection therefore preserves curvature rank by congruence; flat-direction creation/destruction is a source-curvature/reanchoring event. On `ker Q` the global max-normalization face vanishes exactly, and a differentiable right-persistent maximum must satisfy `P0 K_* P0 >= 0`. Exact periodic referees realize both persistent flatness (`K=0`) and pure diffusive kernel opening (`K_zz=24 nu`). First-bad identification, higher-order kernel exhaustion, support/refinement collapse, restart and regularity remain Open-literal/Open.

The scalar curvature-kernel programme has now been lifted to the intrinsic normalized-vorticity map `V=omega/sqrt(2M)`, whose image lies in the unit ball and whose squared radius is exactly `g=e/M`.  At an active maximum the forced inward contact form satisfies `H_c=-V dot Hess(V)=(grad V)^T grad V+Q/2`; hence `ker H_c=ker Q intersect ker(grad V)`.  The tangent-gradient sector is not an added score: its left Gram is exactly the orientation-complete Kelvin q.v. tensor divided by `4 nu M`, while its right Gram carries the same bulk trace in physical derivative directions.  An exact periodic elliptically polarized heat-shear family transfers contact continuously from scalar amplitude curvature to orientation twist while keeping `H_c,zz=k^2`.  At the helical endpoint `g` is identically one and every scalar spatial jet vanishes, yet `grad V`, contact, and Kelvin bulk remain nonzero.  Thus a universal higher-scalar-jet exhaustion is ruled out; the sharper Open-literal seam is closure of the unit-ball contact kernel at an actual first-bad state.  No restart/continuation/regularity theorem is claimed.

The current frontier has now been compressed below the separate Kelvin/Cauchy/contact descriptions.  Pulling the physical vorticity two-form back by the actual Lagrangian flow gives an exact Hodge heat equation `partial_t bar_beta=nu Delta_G bar_beta` on the pullback metric `G=F^T F`, with `d bar_beta=0`; the same metric is flat and volume-one and evolves by `partial_t G=L_U G`, where `U` is the velocity Hodge-reconstructed from the same pulled-back vorticity state (modulo the conserved harmonic/Galilean mode).  This exposes an exact reciprocal metric lock: for `H=cof F=F^-T`, `H^T H=G^-1` and the old packet metric `(H^T H)^-1` is exactly `G`; physical vorticity amplitude uses `G`, while the material diffusion principal symbol uses `G^-1`.  Vortex stretching is precisely time variation of the `G`-fiber norm; equivalently it is the time-Hodge commutator `(partial_t *_G)bar_beta`, while the viscous/Kelvin/covariance Gram is the single Hodge--Bochner second-order product defect.  The combined pairing law has only metric/Hodge work minus the carré-du-champ Gram.  The normalized-vorticity contact identity is its untraced covariant form, not a new primitive.  Deterministic Cauchy/Nanson/full material-current shape are induced representations of the same diffeomorphism.  The new research frontier is therefore the self-generated `(G,bar_beta)` compatibility problem: whether these exact constraints forbid a singular escape remains Open; no restart, continuation, blow-up exclusion, or global-regularity theorem is claimed.  See `docs/material_hodge_bochner_master_compatibility.md`.

The material master compatibility has now compressed one level further: `Gdot/2` and `bar beta/2` are not two independent sectors but the symmetric and antisymmetric halves of the single covariant gradient `nabla^G bar alpha`.  Flatness forces a local integrability relation between their derivatives, and the induced Hodge–strain map (insensitive to the parallel harmonic/Galilean mode) is a scaled `L^2` isometry: `||Gdot||_2^2=2||bar beta||_2^2`, with the same factor at every fixed covariant derivative order.  This identifies total enstrophy with one quarter of material metric-speed energy and viscous enstrophy loss with one half of metric-speed roughness.  The degree-two null-Lagrangian compatibility is exactly the pressure law `-Delta_G pbar=|Gdot/2|^2-|bar beta|^2/2`, so pressure is gauge for closed circulation but a constraint potential in the symmetric strain sector.  The degree-three determinant/Piola compatibility is the Betchov law, making vortex stretching and cubic strain self-amplification two projections of the same integrable-gradient minor.  The frontier is now whether an exact form undergoing Hodge heat can drive its Hodge-isometric compatible flat metric tangent into escape without violating these simultaneous gradient/minor constraints.  No blow-up exclusion, restart, continuation, or regularity theorem is claimed.  See `docs/hodge_strain_null_lagrangian_compatibility.md`.

The Bernoulli/pressure sector also collapses into the same moving Hodge structure.  Let `P_G` be the material Hodge projector onto co-closed one-forms.  Since `P_G bar alpha=bar alpha`, fixed-geometry heat preserves that range, and the exact momentum equation has only an exact complement, differentiation gives `d bar B=(dot P_G)bar alpha` and therefore `(partial_t-dot P_G)bar alpha=nu Delta_G bar alpha`.  Thus pressure/Bernoulli is the off-diagonal connection required to remain in a Hodge subspace that the self-generated metric is moving; vortex stretching is independently the variation `dot *_G` of the Hodge-star readout.  At frozen flat geometry the Hodge–strain map commutes with diffusion, so the full nonlinear symmetric strain/rotation/pressure-Hessian combination is exactly the time variation of that Hodge–strain reconstruction.  The shortest current grammar is therefore: **Hodge heat inside a Hodge geometry moved by the same heated state**.  Its no-escape sufficiency remains Open.

The material Hodge picture has compressed again to a single universal conjugacy law.  For `T_t=Phi_t^*`, every diffeomorphism-natural Hodge operator satisfies `A_G=T_t A_0 T_t^-1`, hence `A_Gdot=L_U^out A_G-A_G L_U^in`.  The earlier pressure projector motion, Hodge-star stretching, Hodge-strain feedback, moving Laplacian, Biot--Savart reconstruction and carré-du-champ are therefore components of one Lax/naturality law, not separate mechanisms.  Because `G=Phi_t^*g_0`, `Delta_G` is unitarily conjugate to the fixed flat-torus Laplacian: its spectrum, spectral gap and intrinsic heat geometry are exactly fixed even when the coefficient matrices `G,G^-1` become highly anisotropic in material coordinates.  Thus a candidate singularity cannot be an intrinsic degeneration of the material diffusion geometry; it must be unbounded distortion of the self-generated conjugating diffeomorphism.  The scaled Hodge-strain isometry further gives `bar beta=S_G^* Gdot`, so the apparent `(G,bar beta)` pair is one flat volume-one metric path plus its compatible tangent, whose pulled-back vorticity tangent obeys Hodge heat.  The open no-escape question is whether such an actual NS-generated self-conjugate Hodge-heat path can lose bounded diffeomorphism distortion in finite time.  No blow-up exclusion, restart, continuation or regularity theorem is claimed.  See `docs/self_hodge_conjugacy_lax_compatibility.md`.

The current no-escape frontier has now been compressed to the canonical Hodge
critical order.  In the fixed-Hodge/Leray gauge, NS is exactly a self-generated
skew transfer plus the positive Stokes operator: `u_t+T_u u+nu A u=0`,
`T_u^*=-T_u`, `A=-Delta`.  Every Hodge spectral quadratic obeys one commutator law,
so stretching is the `F=A` frequency-transfer shadow while total kinetic energy has
no nonlinear production.  In three dimensions `A=curl^2`.  The positive critical
half-derivative form `<u,|curl|u>/2` and signed helicity `<u,curl u>/2` therefore
differ only by the sign of the intrinsic curl spectrum.  Nonlinearity changes the
positive and negative chiral critical contents at exactly the same rate, while
viscosity dissipates each separately: all positive-critical nonlinear growth is
paired opposite-chirality transfer.  A mixed-helicity finite-mode referee activates
this transfer with zero nonlinear energy/helicity production, and amplitude scaling
rules out any universal instantaneous `critical transfer <= viscosity` mechanism.
Thus a genuine no-escape theorem, if it exists, must constrain the cumulative
causal paired-helicity transfer generated by the same velocity, not add another
snapshot score or channel.  No blow-up exclusion, restart, continuation or global
regularity theorem is claimed.  See `docs/critical_hodge_chirality_transfer.md`.

The paired-chirality law also has an exact causal form.  If
`Theta(t)=int_0^t tau`, then the same `Theta` equals
`K_+(t)-K_+(0)+nu int D_+` and `K_-(t)-K_-(0)+nu int D_-`: cumulative critical
transfer cannot disappear and must be resolved in both chirality sectors as stored
critical content or viscous critical dissipation.  Hence critical escape requires
`Theta -> +infinity`, and even a terminally one-sign escape forces infinite
participation of the opposite sign through storage or dissipation.  Meanwhile the
ordinary energy law gives the sharp intrinsic interpolation `K^2<=E Z` and
`int K^2 dt <= E(0)^2/(2nu)`, so any such infinite cumulative transfer must
concentrate into shrinking time windows.  This still does not prove no-escape; it
pinpoints the missing theorem as a dynamic anti-concentration law for the
self-generated paired transfer.

The operator frontier has compressed below the critical chirality description.  With `alpha=u^flat`, `beta=d alpha`, literal incompressible NS is the single exact de Rham current law `beta_t+d(i_u beta+nu delta beta)=0`; after the harmonic mode is fixed, `u=B beta`, so the state is one exact self-induced two-form.  In three dimensions `C=*d=curl`, `C^2=-Delta`, and the projected momentum law is `alpha_t=R_beta alpha-nu C^2 alpha`, where `R_beta eta=-P i_{eta#} beta` is skew and annihilates `C alpha`.  Energy cancellation, Euler-helicity cancellation, advection/stretching, curl chirality, and the previous paired critical transfer are therefore shadows of one alternating skew-square law.  At the critical order `Lambda=|C|`, the dangerous transfer and the viscous sink have an exact common positive pair kernel: transfer is the oriented correlation `(omega(x)-omega(y)).(u(x) cross u(y))`, while viscosity is `nu|omega(x)-omega(y)|^2` on that same kernel.  Thus the remaining no-escape question is whether the self-induced Biot--Savart velocity can sustain infinite finite-time correlation with the very vorticity increments simultaneously charged by the Hodge-adjoint square.  This correlation/anti-concentration bridge remains Open; no continuation or regularity claim is made.  See `docs/derham_skew_square_critical_current.md`.
The same common-kernel identity gives a strict over-quotienting no-go: `u -> -u` preserves every even critical magnitude and pair square but reverses the sign of the oriented transfer `tau`.  Thus any future no-escape law must retain signed/oriented phase correlation; a norm-only or positive-bank architecture cannot even recover the instantaneous direction of critical transfer.
