# Intrinsic enstrophy chamber lifted to Kelvin/Nanson support

This audit continues the intrinsic max-normalized enstrophy localization milestone without adding a human-designed badness score.  It asks whether an Eulerian chamber generated by the exact Navier--Stokes enstrophy law, when equipped with the same physical orientation-complete Kelvin/Nanson packet state, already forces physical support locality.

The answer is **no** in an exact smooth periodic Navier--Stokes calibration. Scalar enstrophy localization controls the normal direction of a level set, while a physical packet also carries support tangent to that level set. The missing geometry face is therefore not a new score but

\[
B_\parallel=PBP,\qquad B=LL^T,\qquad P=I-n\otimes n,
\]

with `L` the coherent physical line frame and `n` the intrinsic level-set normal.

No first-bad identification, restart, continuation, or regularity theorem is claimed.

## 1. Literal periodic Navier--Stokes first

Use the exact heat shear

\[
u=(nA e^{-\nu n^2t}\sin(ny),0,0).\]

Its nonlinear term vanishes, pressure may be constant, and the remaining equation is the heat equation. Hence it is a literal smooth 3D incompressible periodic NS solution. Its vorticity and enstrophy are

\[
\omega=(0,0,-A n^2e^{-\nu n^2t}\cos ny),
\]
\[
e=\tfrac12A^2n^4e^{-2\nu n^2t}\cos^2(ny).
\]

Therefore

\[
M=\max e=\tfrac12A^2n^4e^{-2\nu n^2t},\qquad
\boxed{g=e/M=\cos^2(ny)}.
\]

**Classification: Exact Navier--Stokes identity / audited periodic calibration.**
## 2. Intrinsic chamber geometry
Fix `0<alpha<pi/2` and define the intrinsic level `theta=cos^2(alpha)`.
The connected level chamber has normal span 2 alpha/n and extends in x and z.
For this one-mode family the intrinsic compatibility defect is exactly zero on every regular chamber boundary.
Hence scalar chamber motion is materially compatible in its normal direction; no external motion law is inserted.

**Classification: Exact intrinsic-localization identity / audited NS cancellation.**
## 3. Orientation-complete packet lift

Inside the same chamber take `L=diag(1,2 alpha/n,1)`, so `H=cof L=diag(2 alpha/n,1,2 alpha/n)`.
The packet volume tends to zero, but the face normal to y has physical area exactly one for every alpha.
The support tensor is `B=L L^T=diag(1,4 alpha^2/n^2,1)`.
With level normal e_y, `P=I-e_y e_y^T=diag(1,0,1)`, hence `P B P=diag(1,0,1)` independent of alpha.
This is the exact tangent-plane support left untouched by the scalar chamber width.

**Classification: Exact Nanson/cofactor/support identity.**
## 4. Kelvin current collapses while one physical face stays large

At the chamber maximum y=0, `q0=-A n^2 exp(-nu n^2 t)`.  The only nonzero circulation orientation is the xy face.
Its exact centered circulation is `K_z=-2 A n exp(-nu n^2 t) sin(alpha)`.
The local Stokes target is `-2 A n exp(-nu n^2 t) alpha`, so the physical residual is `r_z=A n^2 exp(-nu n^2 t)(alpha-sin(alpha))/alpha`.
Therefore `r_z -> 0` quadratically as alpha tends to zero, while the middle area entry of H stays equal to one.
That persistent face is the zx face, normal to y.  Since omega is parallel to z, both its Kelvin flux and its local vorticity-flux target are exactly zero.
Hence the O(1) geometric face is literally invisible to the vorticity-current channel: circulation component, target component, and raw error component are all zero.

**Classification: Exact Kelvin/Stokes identity / physical flux-blindness no-go mechanism.**
## 5. The local stochastic and finite-shape channels also become quiet

At y=0 the vorticity gradient vanishes exactly, so the orientation-complete local Kelvin q.v. matrix is zero.
The centered finite-current residual is even in a common y-translation of the anchor, so its anchor derivative at y=0 also vanishes; the full codeforming noise response is zero.
Thus the surviving transverse support is not being signaled by an omitted Brownian source.
The finite-support endpoint drift defect tends to zero as the chamber thickness shrinks.
The exact local Nanson line-frame rate also tends to zero in the same nested-chamber limit.
More strongly, the tangentially projected support rate is identically zero in this calibration.
**Classification: Exact finite-shape and Nanson identities / audited calibration.**
## 6. Orientation-complete quadrupoles expose what circulation misses
For a coherent three-face packet the exact closure is `sum_i Q_i/A_i=(2/3) B`.
Here the face normal to y has normalized quadrupole `diag(1/3,0,1/3)`, independent of alpha.
Therefore the same physical face whose Kelvin flux is identically zero still records both transverse support directions.
This is why the missing face is geometric rather than an additional stochastic-current source.
**Classification: Exact orientation-complete quadrupole reconstruction / rigorous physical typing.**
## 7. Ancestry readout remains a separate physical layer
For the stationary uniform y ancestry marginal, both the probability-current normal speed and fluid normal speed are zero.
The intrinsic level-slip contribution is zero because scalar compatibility vanishes, so the exact net ancestry mass flux is zero.
No transverse geometry is supplied by an unaccounted moving-readout flux.
**Classification: Exact Reynolds/continuity consequence in the audited calibration.**
## 8. Exact nested-chamber no-go
The limit alpha -> 0 is taken at fixed smooth physical time and fixed exact NS solution; it is a localization-filtration limit, not a singular-time limit.
The chamber thickness and packet volume tend to zero, and the physical Kelvin residual tends to zero as well.
The limiting support keeps unit x and z directions even though its y direction collapses.
Hence scalar compatibility and Kelvin-readout collapse do not imply full physical support locality.
**Classification: Audited exact-NS calibration / rigorous insufficiency no-go.**
## 9. Architecture consequence
The scalar chamber law governs normal level-set motion, while Kelvin current governs vorticity flux through oriented faces.
Tangent-plane packet size is carried exactly by the physical support tensor and orientation-complete face quadrupoles.
A future first-bad theorem must therefore couple intrinsic level geometry to the physical refinement/support lineage; it cannot infer locality from scalar compatibility or current residual alone.
**Classification: Rigorous architecture consequence of exact physical typing.**
## 10. Explicit frontier
Open-literal: no theorem yet identifies this nested intrinsic chamber/packet sequence with the programme actual first-bad state.
Open-literal: the NS mechanism that would force tangential support/refinement collapse from a genuine first-bad compatibility obstruction is not proved.
Open: uniform singular-time packet support, conditioning, finite-shape control, restart capacity, continuation, and global regularity remain unresolved.
The chamber width alpha/n is not identified with a Kelvin diffusion length merely because both can be small.
No restart/continuation/regularity theorem claimed.
